# JaxCards 專案交接資訊(v1.6.3)

## 專案是什麼
一個叫 **JaxCards** 的 PWA,信用卡資料管理工具,自己用,也打算分享給朋友使用。
- 卡片用縮圖排列呈現(拍照的卡面照片,或沒照片時的淡紫色卡片+置中名稱),點進去看完整卡號、期限、CVV、備註
- 卡號、期限輸入時都有即時格式化與驗證
- 完全離線,沒有任何網路請求
- 部署在 GitHub Pages:`https://mightylordj.github.io/JaxCards/`(repo 是 Public,免費版 GitHub Pages 只支援 Public repo 這樣才能用)

## 技術架構
- 純 HTML/CSS/JS,沒用框架,四個檔案都在 repo 根目錄(沒有子資料夾):
  - `index.html` — 畫面結構 + 所有樣式(inline `<style>`)
  - `app.js` — 全部邏輯
  - `sw.js` — Service Worker
  - `manifest.json` — PWA manifest
  - `apple-touch-icon.png` — 唯一保留的圖示(其餘 icon-192/512/maskable 已刪除,因為只用 iOS Safari 加入主畫面,iOS 只認這張)
- `sw.js`:`CACHE_NAME` 目前是 `cardvault-shell-v22`,有 Navigation Preload + stale-while-revalidate(同源 GET 先回快取、背景更新),`controllerchange` 時自動 `location.reload()` 讓新版靜默生效——這套做法直接照搬 JaxMoney 的模式
- **重要教訓**:`sw.js` 的快取版本號**只要 `index.html`/`app.js`/`manifest.json`/圖示路徑任何一個檔案有改,就一定要跟著跳號**,不然使用者手機上會卡著新舊檔案混用的快取(例如新版 HTML 配舊版 JS,某個元素 ID 對不上直接讓整支 script 噴錯、整頁變黑屏)。已經真的踩過這個坑一次,修法是使用者要手動去 Safari 設定清該網域的網站資料。
- App 內右上角有版號徽章(目前 `V1.6.3`),跟 `sw.js` 的 `CACHE_NAME` 版本號是兩條獨立編號,但**原則上每次改動都要兩個一起跳**,不要漏掉任何一個。

## 資料模型 / 加密
- IndexedDB `cardvault-db`:
  - `meta` store(key `"config"`):目前只有 `{ autoKey }`
  - `cards` store:`{ id, iv, cipher, photoIv, photoCipher, createdAt, order }`——`order` 是新加的欄位,明文存放(非加密 payload 內),用來記錄使用者手動排序後的順序
- 每張卡片的 JSON payload(`nickname`, `number`, `expiry`, `cvv`, `note`)用 AES-256-GCM 加密,照片(小尺寸 JPEG dataURL)也是同一把 key 加密後存
- 卡號**完整儲存**(使用者自己的選擇,方便網購複製貼上),詳情頁直接顯示,不遮罩,旁邊一顆小圖示按鈕可複製,20 秒後自動清剪貼簿
- **CVV 已經加回來了**(選填欄位,3-4 碼),存在加密 payload 裡,但詳情頁**不會直接顯示真實 CVV**——見下方「CVV 混淆機制」

## ⚠️ 目前沒有任何存取控制,這是暫時狀態
- 已經明確決定**不做 Face ID**:iOS Safari 上 WebAuthn 流程一定會先跳出系統層級的「確認要用 passkey 嗎」按鈕,這顆按鈕不好按、拿不掉,體驗比純打 PIN 還笨重,所以整個 Face ID 方向放棄
- 未來只打算做 **PIN-only**,搭配一顆卡匣頁的設定按鈕,讓使用者自己選擇要不要開啟 PIN 驗證(預設建議開啟,關閉前要求輸入正確 PIN 才能關,避免手機被順手拿去關掉保護)
- **目前這套 PIN 機制還沒開始動工**,開機直接用一把自動產生、沒有任何密碼保護的 key 進卡匣,這把 key 明文存在 IndexedDB 裡
- **分享給朋友使用前,這是最優先要補上的東西**——朋友的手機使用習慣無法掌握,不能只靠「我自己的手機不會被拿走」這個假設

### Face ID 曾經踩過的坑(不要重蹈覆轍)
- 試過「整個鎖定畫面貼一層看不到的感應層,點哪裡都算數」——結果蓋住了 PIN 鍵盤,還忘記加 `touch-action:manipulation`,把雙擊縮放的問題搬回鎖定畫面
- 結論:iOS/Android 都要求「真實的一次手指點擊」才能跳出 Face ID,而且 WebAuthn 在 iOS 上還會多一層系統確認按鈕(不是純生物辨識 API,是 passkey 選擇流程),這一層拿不掉,所以最後放棄 Face ID

## 卡匣頁面(主要打磨對象)
- 已拿掉「離線運作」提示 banner,畫面更乾淨
- 版號徽章旁邊有一顆「備份與還原」圖示按鈕
- 版號那排下方另起一排,放「調整順序」圖示按鈕(單獨一排,不跟標題/版號/備份擠在同一行)
- 縮圖是格狀排列(CSS Grid,`auto-fill, minmax(140px,1fr)`,`align-content:start`——這行必加,不然 Grid 預設會把每列撐開填滿剩餘高度,行距會爆炸大)
- 有拍照的卡片:縮圖只顯示照片,沒有任何文字疊加
- 沒拍照的卡片:淡紫色漸層底,卡片暱稱置中顯示、字級 16px 加粗,最多兩行超過省略
- **品牌偵測邏輯(靠卡號猜 VISA/Mastercard/JCB)已經整個刪除**——原本只用在詳情頁一個文字標籤,但使用者反應這個「自動猜測」的文字容易被誤認成真實欄位,乾脆整個 `detectBrand()` 函式跟相關 CSS 都砍掉,不是只隱藏
- 卡片顯示順序:預設新到舊(依建立時間);使用者手動排序過後,改用 `order` 欄位排序

## 詳情頁(點卡片後)
- 最上面那塊固定用深紫漸層 + 白字,是整頁視覺焦點,不隨卡片品牌變色(品牌標籤已移除,不會再顯示 VISA/卡片 這類文字)
- 卡號同一行顯示完整號碼 + 小圖示複製按鈕,`white-space:nowrap` 保證不換行
- **CVV 混淆機制(自訂設計)**:每次打開詳情頁,從 `{-1, 1}` 隨機抽一個偏移量,把真實 CVV 的每一位數字各自做 `+offset mod 10`(單位數循環進位,例如 0-1=9、9+1=0)後顯示——顯示的不是真的 CVV。真正的偏移方向藏在下面那行「複製的卡號會在 20 秒後自動…」的提示文字上:**點的位置代表解密時要做的動作**(句首出現點 = 要用「減」才能還原成真實 CVV;句尾出現點 = 要用「加」)。原本設計是偏移量可以到 ±2(用點的數量表示幅度),後來簡化成只有 ±1,因為兩個點看起來太顯眼、太可疑
- 備註欄不再用框線框起來,單純文字顯示,不會讓人誤以為可以點進去輸入
- 沒有持卡人欄位、沒有發卡銀行欄位;沒有關閉按鈕,點空白處自動關閉
- 刪除是兩段式確認(第一次點「刪除」變成實心紅底「再按一次確認刪除」,3 秒內沒再點會自動恢復)

## 新增/編輯表單
- 欄位:卡片暱稱(placeholder「台銀Visa卡」)、卡號、有效期限、**CVV**(labeled `CVV 🔒`,沒有任何說明文字,避免洩漏顯示時會被動過手腳)、備註(無 placeholder)
- 卡號:輸入時即時每 4 碼加空格,上限剛好 16 碼
- 有效期限:`inputmode="numeric"`,月份即時鉗制在 01–12,存檔前驗證「沒有過期、沒有超過現在起 15 年」
- **照片欄位縮圖**:固定高度(110px)、寬度用高度乘上卡片比例(1.586)算出來,不是撐滿整個表單寬度撐大變形
- **照片裁切畫面(新功能)**:選完照片後不會直接存,先進入一個全螢幕裁切步驟——固定一個跟卡片同比例(1.586:1)的裁切窗,拖曳移動照片位置、兩指縮放調整大小,調整好按「完成」才擷取那塊區域存起來
  - 裁切畫面下方有一顆**旋轉按鈕**(順時針 90 度),點一下把工作用畫布轉向,轉完自動重新置中貼合裁切窗
  - **效能修正**:手機拍的原始照片解析度通常 4000×3000px 以上,原本直接把這張原圖塞進 `<img>` 標籤持續做 CSS transform,導致連續觸控時整頁卡頓甚至當機(變黑屏/白屏)。修法是選完照片後先在背景用 canvas 把原圖縮小到最長邊 1400px(優先用 `createImageBitmap` 解碼,效率較好,沒有這個 API 才退回舊式 `Image` 讀法),後續顯示、拖曳、縮放、最終裁切輸出都只碰這張已經縮小過的工作畫布,不再碰原始巨圖
- 整頁**故意不能捲動、不能拖曳**(`overflow:hidden`),欄位間距故意壓得比較緊湊——已知取捨,欄位變多或小螢幕鍵盤跳出來蓋住儲存按鈕時要重新考慮

## 備份與還原(新功能)
- 卡匣頁面版號旁邊有一顆備份圖示按鈕,點開是「備份與還原」面板
- **匯出**:先請使用者設定一組密碼(至少 6 碼,輸入兩次確認一致),用這組密碼透過 **PBKDF2(SHA-256,600,000 次迭代)衍生出 AES-256-GCM 金鑰**,把整包資料(`meta` + 所有卡片)加密後包成 JSON 檔案下載,檔名 `jaxcards-backup-YYYY-MM-DD.json`
- **匯入**:選好檔案後輸入密碼才會嘗試解密;密碼打錯的話 AES-GCM 驗證會直接失敗,顯示「密碼錯誤,或檔案已損壞」,不會誤導使用者以為匯入成功。解密成功後會跳原生確認框告知會覆蓋幾張卡片,確認後才真的寫入,並重新整理畫面
- 這個備份機制主要是為了「換手機資料要搬家」的情境——iOS 加到主畫面的 PWA 雖然不會被 7 天沒使用自動清除(有加到主畫面就豁免這個機制),但換新手機時能不能正確還原,取決於系統備份還原,沒有官方保證,所以額外做了這個匯出/匯入功能當保險

## 卡片順序(新功能)
- 卡匣頁面版號那排下方,另一排有一顆「排序」圖示按鈕(只有圖示,沒有文字標籤)
- 點開是全螢幕排序畫面:每張卡片一列(小縮圖+暱稱+右邊的拖曳手把圖示),**只有拖曳手把那塊區域會攔截觸控**,清單本身仍可正常上下捲動
- 拖曳手把的實際熱區加大到 44×44px(用負 margin 往外擴,不影響視覺排版),整個排序畫面都關閉文字選取跟 iOS 長按選單(`user-select:none` + `-webkit-touch-callout:none`),避免拖曳時不小心選到卡片名稱文字
- 按住手把拖曳會即時跟其他卡片交換位置(用 Pointer Events 實作,DOM 順序本身就是排序結果的來源),按「完成」才把當下順序寫回每張卡片的 `order` 欄位
- 主畫面卡匣會依 `order` 欄位排序;還沒手動排序過的卡片,沿用建立時間新到舊排列

## 視覺風格
- 配色比照 JaxMoney,紫色系主色 `--accent:#5E5CE6`
- 主標題「JaxCards」字體也比照 JaxMoney:17px、粗體、字距收緊(`-0.2px`)、顏色用主題紫

## PWA / 行動裝置相關的技術修正(都是實測踩坑後修的)
- `touch-action: manipulation` 全域套用在 `html` 跟所有可點擊元件上(裁切畫面跟排序畫面的拖曳熱區則個別覆寫成 `touch-action:none`,因為那兩處需要自己接管手勢)
- 所有表單輸入框字級強制 ≥16px,避免 iOS 在欄位聚焦時自動放大整頁
- `html`/`body`/`#app` 都鎖在 `100dvh` + `overflow:hidden`
- 曾經試過用 JS 算 `screen.height` 去覆蓋 `#app` 高度,結果跟 `body` 原生 `100dvh` 不同步,反而在工具列收合時露出白色縫隙——已移除,`#app` 現在單純跟 `body` 一樣用原生 `100dvh`
- `--bottom-safe`(約 64px + `env(safe-area-inset-bottom)`)是全站共用的底部保留區

## 目前還沒做 / 之後可以優化的方向
- **PIN 機制一定要重新加回來**,現在完全沒有存取控制,這是分享給朋友使用前最優先要做的事(包含閒置自動鎖定,搭配卡匣頁的一顆設定按鈕讓使用者自己選擇要不要開啟)
- Android/桌面瀏覽器的安裝圖示品質,現在的 manifest 只有一張 `apple-touch-icon.png`,是刻意只服務 iOS 的簡化做法
- 多裝置同步這件事還沒有,目前每支裝置各自獨立,備份/匯入功能是目前搬家用的解法
- 分享給朋友使用前,建議在朋友第一次使用時老實告知目前狀態(卡號完整顯示不遮罩、目前沒有 PIN 保護),讓對方知情後自己決定要不要用
